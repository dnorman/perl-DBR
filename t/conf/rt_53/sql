CREATE TABLE test_abc (
  id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  xyz_id INTEGER
);

CREATE TABLE test_xyz (
  id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  someval INTEGER NOT NULL
);

INSERT INTO "test_abc" VALUES(NULL,NULL);
INSERT INTO "test_abc" VALUES(NULL,2);

INSERT INTO "test_xyz" VALUES(NULL,111);
INSERT INTO "test_xyz" VALUES(NULL,222);
